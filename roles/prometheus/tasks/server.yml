- name: Copy Prometheus Config
  ansible.builtin.template:
    src: prometheus.yml
    dest: /opt/prometheus/prometheus.yml
  vars:
    labels:
      - { tag: "env", label: "environment" }
      - { tag: "Name", label: "name" }

- name: Copy Record Rules
  ansible.builtin.template:
    src: record-rules.yml
    dest: /opt/prometheus/record-rules.yml

- name: Restart Prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: restarted

### Grafana
# Commented to do for every instance & moved this code common folder i.e.,
#  whenever components are created grafana templates will also get created
#- name: Make Template
#  ansible.builtin.template:
#    src: grafana-template.json
#    dest: /tmp/grafana.json
#    remote_src: yes
#
#- name: Import Grafana dashboard
#  community.grafana.grafana_dashboard:
#    grafana_url: http://172.31.13.17:3000
#    grafana_api_key: "{{ grafana_api_key }}"
#    state: present
#    overwrite: yes
#    path: /tmp/grafana.json